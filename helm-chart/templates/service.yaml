apiVersion: v1
kind: Service
metadata:
  name: {{ include "durianlab-frontend.fullname" . }}
  labels:
    {{- include "durianlab-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "durianlab-frontend.selectorLabels" . | nindent 4 }}